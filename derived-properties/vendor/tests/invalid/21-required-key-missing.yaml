# Description is required, because it is what the log, the documentation
# and the property dictionary all show, and no other field can stand in.
Script: |
  Format: 1
  Name: Broken
  Version: 1.0.0
  Output:
    Name: Broken
    ValueType: string
    IsList: false
  Rules:
    - Else: High
Expect:
  Paths: [ "Output.Description" ]
  Mentions: [ "required key 'Description' is missing" ]
